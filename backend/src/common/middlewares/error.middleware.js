/**
 * Backwards compatibility re-export.
 * Canonical error middleware lives in src/middleware/error.middleware.js.
 */
export { errorMiddleware, notFoundHandler } from '../../middleware/error.middleware.js';
